import React from 'react';
import preloader from "../../../assets/preloaders/spinning-circles.svg";

let Preloader = (props) => {
    return <img style={{margin: '20px'}} src={preloader}/>
};

export default Preloader;